[package]
name = "rust-test"
version = "0.0.0"
edition = "2021"
publish = false

[features]
default = ["cpp"]
cpp = []

[build-dependencies]
cc = "1"
# TODO: 0.1.49 fails for android
cmake = "=0.1.50"
